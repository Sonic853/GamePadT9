# GamePad T9 独立运行版

本包内置 Rime 1.17.0 引擎（2026-09-10 核对的官方最新稳定版）、简体九键词库、方案和匹配的混合拼音索引，**无需安装小白 T9**，首次运行无需联网或部署词库。小白联动保留为可选功能。

仍然不包含 .NET 运行时。目标电脑需要 **.NET 10 桌面运行时（Windows x86）**，即使系统是 x64，也需要 x86 桌面运行时。支持 x64 Windows 10/11 和 x64/x86 目标程序，不支持 ARM64。Rime DLL 的其他依赖均为 Windows 系统组件。

将整个文件夹解压到可写目录，运行 `Start.cmd` 或 `GamePadT9.exe`。不需要管理员权限，不会自动注册或注入输入法。`runtime/rime` 中是内置引擎与词库，`components` 中是可选输入组件，请保留整个目录。

## 未安装输入法组件时

独立版全局默认使用外部输入框，完成方式为“完成输入后填入至目标程序”。先将光标放在目标程序的文本框，同时按 View + Menu 开启输入；选词后继续编辑，长按 A 或再次按 View + Menu 完成。完成时若未注册 GamePad T9 或注入小白 T9，则复制到剪贴板、关闭输入并通过托盘提示手动粘贴。仅安装原版小白不等于已注入。

这个方式不需要 GamePad T9 或小白输入法条目。已保存的全局/独立程序配置按原值加载，不会自动改成剪贴板模式；可在“程序列表”调整。启动不会强制打开组件注册窗口。

## 自动填入目标程序

在“设置 → 输入法组件”选择一种方式：

- **注册 GamePad T9 输入法**：不需要小白，适用于直接输入、游戏失去焦点、外部输入框完成后填入目标。
- **注入小白 T9 输入**：仅当已安装小白时可选，沿用其输入法条目；手柄组词仍使用本包内置引擎与词库。

注册与注入互斥，需先卸载/还原当前方式再切换。只有组件管理操作需要管理员权限；组件复制到本机 Program Files/GamePadT9，目标程序可能需要重开。启用输入时记录并切换输入法，关闭后恢复。自动填入需要目标控件支持 TSF，不支持时保留文字供复制。

外部输入框的组件检测在完成时进行，不会阻止无组件时开启和编辑。复制回退不改写已保存的完成方式；明确选择“复制到剪切板”时直接复制，不检测组件。已有组件但目标不支持、焦点变化或提交失败时保留文字供处理；“无屏蔽操作”和“游戏失去焦点”仍需输入组件。不会模拟字母或粘贴按键。

## 手柄操作

- Xbox：View + Menu；PS4：Share + Options；PS5：Create + Options 开启/完成输入。
- 默认右摇杆选九格，左摇杆选细分字母，RT/R2 确认；R3 在拼音模式输入整组。
- A/× 选词，无候选时输入空格；外部输入框长按 A/× 完成复制或填入。
- 肩键选候选，DPad 左右翻页，X/□ 退格，Y/△ 循环九键拼音 → 英文 → 数字。
- B/○ 关闭候选或退出英文四格；长按 B/○ 保留草稿并关闭输入。
- 英文逐字输入，无补全；只用右摇杆时确认字母组展开四格，选字母后默认回九格。设置可勾选“输入字母后保留四格”；空白格不输入且保留四格。
- 英文默认右肩键切换大小写，可配置为左肩键；符号菜单中肩键仍选候选。

设置支持已连接手柄选择、左右摇杆/扳机、拼音与英文独立四格排序、可见度和背景模糊。程序列表可单独配置无屏蔽、游戏失焦或外部输入框。

## 数据与更新

内置文件按清单校验，缺失或损坏时提示重新解压，不会转而读取小白安装目录。内置引擎不读取 `runtime-location.json`；小白升级或卸载不会影响内置词库。

个人词频及生成的索引保存在 `data/builtin-user`，设置位于 `user-settings.json` 和 `program-profiles.json`。内置索引完整时直接使用；本地与随包索引都缺失或损坏时可离线重建。请分发原始 ZIP，避免将使用后产生的数据和日志一起复制给他人。

源码构建：`scripts/package.ps1 -Independent`；原来的 `scripts/package.ps1` 仍构建依赖本机小白 T9 的较小便携包。两种包都不包含 .NET，ZIP 均使用 Deflate 极限压缩。输入数据和引擎说明见 `runtime/rime/licenses/SOURCES.md`，完整对应词库源文件作为单独的源码归档提供。
