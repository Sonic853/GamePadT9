# GamePad T9 便携版

本包不包含 .NET 运行时。目标电脑需先安装 **.NET 10 桌面运行时（Windows x86）**，可从 [微软官方下载页](https://dotnet.microsoft.com/zh-cn/download/dotnet/10.0) 的“.NET 桌面运行时”栏目选择 Windows x86。主程序为 32 位，即使系统是 64 位也需要 x86 运行时；仅安装 x64 运行时不满足要求。无需安装 SDK 或开发工具。

将整个文件夹解压到可写目录，运行 `Start.cmd` 或 `GamePadT9.exe`。支持 x64 Windows 10/11，以及 x64/x86 目标程序；本包不支持 ARM64 Windows 和仅提供 x64 Rime 引擎的小白版本。

主程序、`Svg.dll`、`ExCSS.dll`、`SDL3.dll`、`GamePadT9.Backdrop.dll` 及 .NET 启动配置已打包到 `GamePadT9.exe`，不需要在主程序旁放置这些 DLL。SDL3 和背景模糊组件启动时由 .NET 自动释放到当前用户的临时缓存后加载。`components` 中的输入法 DLL 和辅助程序供 Windows 或目标进程独立加载，仍需随整个文件夹复制；本包不是只复制一个 EXE 的完整发行方式。[单文件发布机制](https://learn.microsoft.com/en-us/dotnet/core/deploying/single-file/overview)

目标电脑需要已安装并部署小白 T9。启动时自动读取本机安装目录和用户数据目录，不固定小白版本号；未识别时会出现目录选择窗口。主程序使用本机的 x86 `rime.dll` 和已部署的 `xiaobai_simp` 方案、词库。便携包不包含小白的引擎、词库、用户词频、其他电脑的设置或注册备份。引擎 ABI 或方案名称变化的版本需要先适配，不能保证所有未来版本均兼容。

外部输入框默认“完成输入后填入至目标程序”。完成时如果尚未注册 GamePad T9 或注入小白 T9，则复制到剪贴板、关闭输入并通过托盘提示手动粘贴；仅安装原版小白不等于已注入。复制回退不修改保存的设置，明确选择复制时仍直接复制。已有组件但目标不支持或提交失败时，保留草稿供处理。

在托盘的 **设置 → 输入法组件** 选择一种方式：

- **注册 GamePad T9 输入法**：添加独立 Windows 输入法条目。按钮随后变为“卸载 GamePad T9 输入法”。
- **注入小白 T9 输入**：保留原来的小白输入法条目和原版 DLL，通过标准 TSF 接口增加手柄通道；不替换原版文件，不使用固定版本的小白 IPC。按钮随后变为“还原小白 T9 输入”。

两种方式互斥，启用一种后另一种的安装按钮禁用。先卸载/还原当前方式，才能切换。操作立即生效，与窗口底部的“保存设置/取消”无关。安装与恢复需要当前登录用户的管理员授权；目标程序需重新打开以加载组件，Windows 列表未刷新时注销后登录。

输入组件复制到 `Program Files\GamePadT9`，小白原注册信息备份也保存在该电脑的这个目录中，因此移动便携主程序不会使已注册 DLL 路径失效。还原时只处理仍指向本扩展的注册项；小白更新后已换成新版的注册项会保留。原版 DLL 已被删除时会停止还原并提示，需先修复小白安装。卸载输入法不会关闭其他程序，已加载的组件文件会保留到那些程序退出。

**开始输入**：将光标放在目标文本框，同时按 Xbox 的 View + Menu（PS4：Share + Options；PS5：Create + Options）。默认右摇杆选区，RT/R2 或 R3 输入，A/× 选词，肩键选候选，方向键翻页，X/□ 退格，Y/△ 循环切换九键拼音 → 英文 → 数字，B/○ 关闭候选，长按 B/○ 关闭输入。关闭后恢复原输入法。

**英文输入**：直接输入英文字母，不补全，默认小写。右摇杆选组、左摇杆选字母后按 RT/R2 或 R3 输入，空白格高亮但不输入。只用右摇杆时，首次确认展开并锁定当前字母组，再用右摇杆选四格中的字母；左摇杆有选择时优先。成功输入字母后默认返回九格，可在设置 → 手柄与输入中勾选“输入字母后保留四格”，以连续输入同组字母；确认空白格不输入，也不退出四格。两根摇杆回中时不高亮，确认返回九格，短按 B 也可返回。符号格直接打开常用符号，英文符号排在前面；左摇杆可直接选择半角逗号和句点。短按 A/× 输入空格，外部输入框长按 A/× 或 View + Menu 完成复制 / 填入。英文和空格通过文本通道提交，不模拟字母或 Space 按键。

**英文大小写**：默认点击右肩键 RB/R1 切换，长按只切换一次；可在 **设置 → 手柄与输入 → 英文大小写切换** 下拉框中选择左肩键 LB/L1 或右肩键 RB/R1。九格、四格字母及提示同步显示当前大小写。常用符号菜单中，两枚肩键仍用于上下选择并支持长按翻选，不改变大小写。大小写在本次运行中保留，重新启动默认小写；肩键绑定保存到设置文件。

**四格排序**：在 **设置 → 四格排序** 中分别设置九键拼音和英文，点击可视四格单选，绿色边框表示选中。预览沿用输入界面的紧凑样式，空白格留空，只有自定义块底部显示“自定义”。三种预设为“空白 A / C B”“A B / 空白 C”“A B / C 空白”；选中“自定义”后，可拖动任意格到另一格上交换位置，Esc 取消拖动。勾选英文的“与九键拼音复用”后，英文选项跟随拼音并禁用，取消勾选则恢复英文原来的独立设置。“空白”位置在四字母组中对应第 4 个字母。设置同时影响四格显示及实际输入；符号格、整组输入和数字布局保持不变，符号不参与调整。

没有拼音编码、候选词或待选符号时，短按 **A/× 输入一个半角空格**，适用于直接输入、游戏失焦和外部输入框。数字模式也支持，但暂存着未完成的九键编码时不插入空格。有候选时仍只选词，外部输入框长按 A 完成时不会额外插入空格。空格作为文本提交，不模拟 Space 按键。

**混合拼音**：右摇杆选择字母组，左摇杆推出约 49% 后，高亮格展开为四格，内部顺序为 `4 1 / 3 2`。ABC 显示为 `空白 A / C B`，PQRS 显示为 `S P / R Q`。扳机输入高亮字母；空白同样高亮，确认时输入整组。左摇杆回到约 34% 内收起，恢复整组输入。R3 始终输入整组；数字模式保持原来的 1–9 和按下摇杆输入 0。设置中交换九格摇杆后，另一摇杆负责字母。

符号格展开为上半部“符号”、左下逗号、右下句号。下半部可直接输入中文标点；若有候选，先确认高亮词再输入标点。字母与整组可以交替组词，不模拟字母按键。

设置、程序列表与外部输入框使用 WPF UI 的 Fluent 界面。设置侧栏的“手柄与输入”选择设备及左右摇杆/扳机；“面板外观”调整可见度并实时预览；“输入法组件”管理两种接入方式。WPF UI 依赖也已整合进主程序 EXE。

“面板外观”的三档可见度同时应用于底部输入界面和外部输入框：背景默认 50%，九格及输入栏、普通按钮 80%，高亮及完成按钮 90%。两个界面共用一个背景模糊滑块，0% 关闭、100% 最强，默认关闭；文字和图标保持清晰。旧版分别设置时沿用“输入界面”的模糊程度，保留原有可见度；再次保存会移除旧的外部输入框模糊字段。模糊使用 Windows 的实时背景合成，无需另装图形运行时；系统不支持时保留半透明显示。

“程序列表”左侧选择全局配置或独立程序，可搜索路径、添加 EXE 或选择运行中的窗口；右侧使用下拉框配置无屏蔽操作、游戏失去焦点或外部输入框。全局默认使用外部输入框，完成后填入目标程序；已有全局和独立程序配置按保存值加载。保存后，下次开启输入时生效；取消或 Esc 不写入修改。失焦方式的候选和数字提交无需摇杆回中。

外部输入框在九宫格上方紧凑显示，不显示标题栏，也不使用淡入淡出动画。文本框固定为一行高度，长文本随光标滚动并保留换行；目标程序、完成方式、状态和字数合并为一行。可点击“复制”“清空”“保留并关闭”或完成按钮；短按 A（PS：×）选词，长按 1 秒或再次按 View + Menu（PS：Share/Create + Options）完成输入并关闭，按所选配置复制或填入原目标。Enter 在草稿中换行。完成按钮的 Steam SVG 图标跟随所选手柄；填入失败时保留草稿和窗口，重复按组合键不会重复提交。长按 B、点击“保留并关闭”或关闭窗口时保留本次进程内的草稿，并恢复原目标与输入法。

首次启动会在本文件夹的 `data/local` 中建立本机词库快照和独立学习目录。请优先把原始 ZIP 发给其他电脑，避免把使用后产生的学习数据、`user-settings.json`、`program-profiles.json`、`runtime-location.json` 和 `artifacts` 日志一起分发。

本包附带 `cache/mixed` 混合索引缓存。启动时直接使用本地已有完整缓存；本地缺少时，导入与本机方案和词库匹配的随包缓存，首次启动也无需重新编译。只有两处均无可用缓存时才重新生成，本机生成约一分钟。缓存按方案和词库内容匹配，文件日期、目录搬迁及重新部署相同内容不会触发重建；实际方案或词库不同、缓存缺失或损坏时会重新寻找匹配缓存或生成。不修改小白原方案、词库或 Windows 输入法注册。

ZIP 使用 7-Zip 的 Deflate 极限压缩参数（`-mx=9 -mfb=258 -mpass=15 -mmt=off`），并在打包后检查完整性。Windows 可直接解压，运行电脑不需要 7-Zip。随包缓存增加了压缩包体积，用于减少首次启动等待；其中不包含词库原始文本、个人词频或学习数据。

源码项目的 `scripts/package.ps1` 可重建此发行包。`THIRD-PARTY-NOTICES.md` 与 `licenses` 包含所用库和 Steam 图标的说明。组件管理不是后台手柄屏蔽驱动，不安装虚拟手柄。
